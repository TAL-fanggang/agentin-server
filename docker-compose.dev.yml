services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: agentin
      POSTGRES_USER: agentin
      POSTGRES_PASSWORD: agentin_dev
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
